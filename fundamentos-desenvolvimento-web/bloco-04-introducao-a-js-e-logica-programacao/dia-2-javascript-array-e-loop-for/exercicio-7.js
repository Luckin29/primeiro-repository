let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
let menor = 0;
let maior = 0;

for(let i=0; i<numbers.length;i+=1 ) {
    if ((numbers[i]) > maior ){
        maior = numbers[i];        
    } else {
        maior = maior;
        } }
        
    menor = maior;
for(let i=0; i<numbers.length;i+=1 ) {

if (numbers[i] < menor){
menor = numbers[i]
} else {}
}
console.log(menor);
